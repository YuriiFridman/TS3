# VoiceChat - Голосовой чат по типу Discord/TeamSpeak

Полнофункциональное приложение для голосовой и текстовой связи, написанное на Python.

## 🚀 Возможности

### Основные функции
- **Голосовая связь в реальном времени** между несколькими пользователями
- **Текстовый чат** с поддержкой комнат
- **Система аутентификации** (регистрация/вход)
- **Создание и присоединение к комнатам**
- **Графический интерфейс пользователя** на Tkinter
- **Основные инструменты модерации**

### Функции администратора
- Блокировка пользователей в чате (mute/unmute)
- Блокировка пользователей на сервере (ban)
- Управление комнатами и пользователями

### Технические особенности
- Многопоточная архитектура
- Сохранение пользователей в SQLite
- Логирование событий
- Обработка ошибок соединения
- Поддержка множественных комнат

## 📋 Системные требования

- Python 3.7+
- Windows/Linux/macOS
- Микрофон для голосовой связи
- Звуковая карта или встроенный аудио

## 🔧 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/YuriiFridman/TS.git
cd TS
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### Дополнительная установка PyAudio (может потребоваться)

**Windows:**
```bash
pip install pyaudio
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install python3-pyaudio
# или
sudo apt-get install portaudio19-dev python3-dev
pip install pyaudio
```

**macOS:**
```bash
brew install portaudio
pip install pyaudio
```

## 🚀 Запуск

### 1. Запуск сервера
```bash
python server.py
```

Сервер будет запущен на:
- Текстовый чат: `localhost:12345`
- Голосовая связь: `localhost:12346`

**Учетная запись администратора по умолчанию:**
- Логин: `admin`
- Пароль: `admin123`

### 2. Запуск клиента
```bash
python client.py
```

### 3. Подключение
1. Нажмите "Подключиться" для соединения с сервером
2. Зарегистрируйтесь или войдите с существующими учетными данными
3. Начните общение!

## 🎮 Как использовать

### Текстовый чат
1. Введите сообщение в поле внизу окна
2. Нажмите Enter или кнопку "Отправить"

### Голосовая связь
1. Нажмите и удерживайте кнопку "🎤 Нажмите и удерживайте"
2. Говорите в микрофон
3. Отпустите кнопку для завершения передачи

### Управление комнатами
1. **Присоединение:** Дважды кликните на комнату в списке
2. **Создание:** Нажмите кнопку "Создать" рядом со списком комнат
3. **Обновление списка:** Нажмите "Обновить"

### Команды администратора
Если у вас есть права администратора, доступны дополнительные команды:
- **Заблокировать в чате:** Запретить пользователю отправлять сообщения
- **Разблокировать в чате:** Разрешить пользователю отправлять сообщения
- **Заблокировать на сервере:** Полностью заблокировать пользователя

## ⚙️ Конфигурация

Настройки можно изменить в файле `config.py`:

```python
SERVER_CONFIG = {
    'host': 'localhost',      # IP адрес сервера
    'text_port': 12345,       # Порт для текстового чата
    'voice_port': 12346,      # Порт для голосовой связи
    'max_clients': 50         # Максимальное количество клиентов
}
```

## 📁 Структура проекта

```
TS/
├── server.py           # Основной сервер
├── client.py           # GUI клиент
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости Python
├── README.md          # Документация
├── server_data/       # Папка с данными сервера
│   └── users.db      # База данных пользователей
└── server.log         # Лог файл сервера
```

## 🔧 Устранение неисправностей

### Проблемы с PyAudio
Если возникают ошибки с PyAudio:

**Ошибка: "Microsoft Visual C++ 14.0 is required"**
- Установите Microsoft Visual C++ Build Tools
- Или используйте: `pip install pipwin` и затем `pipwin install pyaudio`

**Linux: "jack server is not running"**
```bash
sudo apt-get install libjack-jackd2-dev
```

### Проблемы с подключением
1. Убедитесь, что сервер запущен
2. Проверьте правильность IP адреса и порта
3. Проверьте настройки файрвола
4. Убедитесь, что порты не заняты другими приложениями

### Проблемы с голосом
1. Проверьте подключение микрофона
2. Убедитесь, что микрофон не заблокирован системой
3. Попробуйте изменить настройки аудио в `config.py`

## 🔐 Безопасность

- Пароли хранятся в хешированном виде (SHA-256)
- Поддержка системы администраторов
- Логирование всех действий
- Защита от спама и флуда

## 🤝 Разработка

### Добавление новых функций
1. Изучите структуру кода в `server.py` и `client.py`
2. Добавьте новые типы сообщений в `config.py`
3. Реализуйте обработчики на сервере и клиенте
4. Обновите GUI при необходимости

### Архитектура
- **Сервер:** Многопоточная обработка клиентов, SQLite для данных
- **Клиент:** Tkinter GUI, отдельные потоки для аудио и сети
- **Протокол:** JSON сообщения через TCP (текст) и UDP (голос)

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Смотрите файл LICENSE для подробностей.

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Устранение неисправностей"
2. Создайте Issue в репозитории GitHub
3. Опишите проблему максимально подробно

## 🔄 История версий

### v1.0.0
- Первоначальный релиз
- Базовая голосовая и текстовая связь
- Система аутентификации
- Графический интерфейс
- Основные админские функции

---

**Создано с ❤️ для сообщества разработчиков**
